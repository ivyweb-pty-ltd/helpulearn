<odoo>
  <data>
    <!-- explicit list view definition -->
    <!--
    <record model="ir.ui.view" id="grading.list">
      <field name="name">grading list</field>
      <field name="model">grading.grading</field>
      <field name="arch" type="xml">
        <tree>
          <field name="name"/>
          <field name="value"/>
          <field name="value2"/>
        </tree>
      </field>
    </record>
    -->

    <!-- actions opening views on models -->
    <!--
    <record model="ir.actions.act_window" id="grading.action_window">
      <field name="name">grading window</field>
      <field name="res_model">grading.grading</field>
      <field name="view_mode">tree,form</field>
    </record>
    -->

    <!-- server action to the one above -->
    <!--
    <record model="ir.actions.server" id="grading.action_server">
      <field name="name">grading server</field>
      <field name="model_id" ref="model_grading_grading"/>
      <field name="state">code</field>
      <field name="code">
        action = {
          "type": "ir.actions.act_window",
          "view_mode": "tree,form",
          "res_model": self._name,
        }
      </field>
    </record>
    -->

    <!-- Top menu item -->
    <!--
    <menuitem name="grading" id="grading.menu_root"/>
    -->
    <!-- menu categories -->
    <!--
    <menuitem name="Menu 1" id="grading.menu_1" parent="grading.menu_root"/>
    <menuitem name="Menu 2" id="grading.menu_2" parent="grading.menu_root"/>
    -->
    <!-- actions -->
    <!--
    <menuitem name="List" id="grading.menu_1_list" parent="grading.menu_1"
              action="grading.action_window"/>
    <menuitem name="Server to list" id="grading" parent="grading.menu_2"
              action="grading.action_server"/>
    -->
  </data>
</odoo>