<odoo>
  <data>
    <!-- explicit list view definition -->
    <!--
    <record model="ir.ui.view" id="instruction.list">
      <field name="name">instruction list</field>
      <field name="model">instruction.instruction</field>
      <field name="arch" type="xml">
        <tree>
          <field name="name"/>
          <field name="value"/>
          <field name="value2"/>
        </tree>
      </field>
    </record>
    -->

    <!-- actions opening views on models -->
    <!--
    <record model="ir.actions.act_window" id="instruction.action_window">
      <field name="name">instruction window</field>
      <field name="res_model">instruction.instruction</field>
      <field name="view_mode">tree,form</field>
    </record>
    -->

    <!-- server action to the one above -->
    <!--
    <record model="ir.actions.server" id="instruction.action_server">
      <field name="name">instruction server</field>
      <field name="model_id" ref="model_instruction_instruction"/>
      <field name="state">code</field>
      <field name="code">
        action = {
          "type": "ir.actions.act_window",
          "view_mode": "tree,form",
          "res_model": self._name,
        }
      </field>
    </record>
    -->

    <!-- Top menu item -->
    <!--
    <menuitem name="instruction" id="instruction.menu_root"/>
    -->
    <!-- menu categories -->
    <!--
    <menuitem name="Menu 1" id="instruction.menu_1" parent="instruction.menu_root"/>
    <menuitem name="Menu 2" id="instruction.menu_2" parent="instruction.menu_root"/>
    -->
    <!-- actions -->
    <!--
    <menuitem name="List" id="instruction.menu_1_list" parent="instruction.menu_1"
              action="instruction.action_window"/>
    <menuitem name="Server to list" id="instruction" parent="instruction.menu_2"
              action="instruction.action_server"/>
    -->
  </data>
</odoo>